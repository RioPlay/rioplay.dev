# Rebuildable artifacts — regenerated by 'aden gen', never commit (ADR-003).
store/
cache/
gen-cache.json
scan-cache.json
staging/

# Durable, repo-coupled intent — DO commit these.
!overlays/
!constitution.adoc
!hooks/

# project.conf holds a machine-specific absolute path — kept local, ignored at
# the repo root (.gitignore). Intentionally NOT re-included here.
project.conf
